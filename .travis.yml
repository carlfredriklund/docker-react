language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t carlfredriklund/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true carlfredriklund/docker-react npm run test